# 📘 دليل المطور الرسمي لتفعيل وتوليد مفاتيح الزبائن (Developer License Keygen Manual)
### 👑 نظام لابريو لإدارة المختبرات الطبية والتشخيص الذكي (Labryo LIMS Pro)
**رقم هاتف وواتساب المطور المعتمد:** `07764271130`

---

## 🔒 1. آلية عمل نظام الحماية والترخيص التلقائي

1. **فترة تجريبية تلقائية (7 أيام)**:
   - عند تثبيت البرنامج لأول مرة عند أي زبون، يقوم النظام تلقائياً بقراءة البصمة العتادية للجهاز (**Hardware ID - HWID**) المستخرجة من اللوحة الأم والمعالج.
   - يمنح النظام المختبر **7 أيام تجريبية مجانية كاملة الميزات** تعمل محلياً (Offline) بنسبة 100%.

2. **القفل التلقائي بعد انتهاء الـ 7 أيام**:
   - عند انتهاء اليوم السابع، ينقفل النظام فوراً وتظهر شاشة حمراء رسمية غير قابلة للإغلاق تحمل الرسالة:
     > **"انتهت الفترة التجريبية للنظام. يرجى الاتصال بالمطور للتفعيل الدائم على الرقم: 07764271130"**
   - تعرض الشاشة كود بصمة الجهاز الخاص بالزبون مع زر **"نسخ الكود"** وزر مباشر للمراسلة عبر واتساب.

3. **الحماية ضد التلاعب بساعة الكمبيوتر (Anti-Time-Tampering)**:
   - إذا حاول الزبون تأخير تاريخ وساعة الكمبيوتر للالتفاف على الـ 7 أيام، يقارن النظام التوقيت مع آخر عينة مسجلة في قاعدة البيانات `lab.db` ويقفل البرنامج فوراً بتنبيه: *"تم اكتشاف تلاعب في ساعة النظام!"*.

---

## 🛠️ 2. كيف يقوم المطور بتوليد مفتاح التفعيل للزبون؟

لديك طريقتان سهلتان لتوليد المفاتيح للزبائن:

### 🌟 الطريقة الأولى: الأداة السريعة بنقرة واحدة (One-Click CLI Tool)
1. افتح مجلد النظام الرئيسي `d:\lab`.
2. اضغط مرتين على الملف:
   👉 **`توليد_مفاتيح_تفعيل_الزبائن.bat`**
3. سيطلب منك:
   - **كود بصمة جهاز العميل (HWID)**: الصق الكود الذي أرسله لك الزبون (مثال: `LAB-8F42-99A1-B330`).
   - **نوع الباقة**:
     - اضغط `1` لترخيص **دائم مدى الحياة (LIFETIME)**.
     - اضغط `2` لترخيص **سنوي (YEARLY - 365 يوم)**.
     - اضغط `3` لترخيص **شهري (MONTHLY - 30 يوم)**.
   - **اسم المختبر**: اكتب اسم مختبر الزبون (مثال: `مختبر الرضا التخصصي`).
4. سيظهر لك مفتاح تفعيل مشفر يبدأ بـ `LIC-...`.
5. **انسخ المفتاح وأرسله للزبون عبر واتساب**.

---

### 🌐 الطريقة الثانية: عبر واجهة الويب الرسومية للمطور (`/license/keygen`)
1. افتح صفحة المطور: [http://localhost:8080/license/keygen](http://localhost:8080/license/keygen) (أو من داخل البرنامج عبر "إدارة التراخيص" -> "بوابة توليد المفاتيح للمطور").
2. الصق كود بصمة جهاز الزبون (HWID).
3. اختر نوع الباقة (شهري / سنوي / مدى الحياة).
4. اكتب كلمة مرور المطور (`admin123` أو `lab@2026`).
5. اضغط **"توليد المفتاح"**، ثم اضغط **"نسخ المفتاح"**.

---

## 📱 3. كيف يقوم الزبون بتفعيل نسخته؟

1. يستلم الزبون المفتاح منك عبر واتساب.
2. في شاشة القفل التي تظهر على جهازه، يقوم بلصق المفتاح في مربع **"أدخل مفتاح التفعيل الدائم"**.
3. يضغط الزبون على زر **"تفعيل النسخة الدائمة ✓"**.
4. يتحقق النظام محلياً أوفلاين من التوقيع الرقمي (HMAC-SHA256) وتطابق بصمة جهازه.
5. **ينفتح النظام فوراً في أجزاء من الثانية ويصبح مرخصاً بشكل دائم** دون الحاجة لأي اتصال بالإنترنت!

---

## 🔐 4. الأمان والتشفير (Security Architecture)
- المفاتيح مشفرة وموقعة رقمياً بخوارزمية **HMAC-SHA256** ومربوطة بالمعالج واللوحة الأم للجهاز.
- **لا يمكن تشغيل نفس المفتاح على جهاز آخر**؛ كل مفتاح يعمل حصراً على الجهاز الذي استُخرج كود بصمته (HWID).
- **لا يمكن تخمين أو تزوير المفاتيح**، وتعمل آلية التحقق بالكامل أوفلاين دون الحاجة لأي خادم وسيط.
